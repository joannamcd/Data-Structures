def twoSum(nums, target):
    seen = {}
    for i in range(len(nums)):
        diff = target - nums[1]
        if diff in seen:
            return [seen[diff],i]
        else:
            seen[nums[i]] = i